%h1 Editing external service #{external_service.name}

= render 'form'

= link_to 'Show', project_external_service_path(project, external_service)
\|
= link_to 'Back', project_external_services_path(project)
