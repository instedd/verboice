[verboice]
exten => h,1,NoOp()
exten => _X.,1,AGI(agi://<%= host_ip %>:19000,,${EXTEN},${CHANNEL(pjsip,remote_addr)})
exten => s,1,AGI(agi://<%= host_ip %>:19000,,${EXTEN},${CHANNEL(pjsip,remote_addr)})
